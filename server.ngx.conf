server {
	server_name errorpage;
	root /var/www/errorpage;

    location /geoip-json {
        default_type application/javascript;
        echo "$arg_callback({\"host\": \"$remote_addr\"});";
    }
}
